import { Image } from 'react-native'

const Images = {
  ic_reload: Image.resolveAssetSource(require('../Assets/Icons/reload.png')),
  noImage: Image.resolveAssetSource(require('../Assets/noImage.png'))
}

export default Images
